#include <stdio.h>

/*
    Corrida: Debes teclear un carácter.
    Variante del ejecicio previo, pero ahora usa una variable
    para almecenar el valor de getchar.
*/

void main(){
    char letra;
    letra = getchar();
    putchar(letra);
}